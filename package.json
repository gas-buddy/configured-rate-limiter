{
  "name": "@gasbuddy/configured-rate-limiter",
  "version": "1.0.0",
  "description": "A configuration driven rate limiter",
  "main": "build/index.js",
  "scripts": {
    "test": "tap --node-arg=-r --node-arg=@babel/register tests/*.js",
    "test-some": "tap --node-arg=-r --node-arg=@babel/register",
    "cover": "nyc --reporter=lcov npm test",
    "lint": "eslint .",
    "build": "babel src --out-dir build",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:gas-buddy/gb-services.git/configured-rate-limiter.git"
  },
  "keywords": [
    "redis",
    "rate limiting",
    "apis",
    "gasbuddy"
  ],
  "author": "GasBuddy",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/register": "^7.4.0",
    "babel-preset-gasbuddy": "^4.2.0",
    "eslint-config-gasbuddy": "^5.1.0",
    "ioredis": "^4.9.0",
    "tap": "^12.6.1"
  },
  "dependencies": {
    "rate-limiter-flexible": "^0.23.5"
  }
}
